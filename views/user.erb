<div class="hm-container">
      <div class="welcomeHome">

    <h2>Welcome to <%= @user.fname %>'s Page!</h2>

    <% @posts.each do |post| %>
          <h3><%= post.title %></h3>
        <p>
            <%= post.content %>
        </p>
    <% end %>
  </div>
</div>
 
